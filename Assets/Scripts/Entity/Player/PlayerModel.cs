using System.Collections.Generic;

public class PlayerModel : EntityModel
{
    public PlayerModel(PlayerController playerController, AttributeCollection attributeList, ParameterCollection parameterList) : base(playerController, attributeList, parameterList)
    {
    }
}
